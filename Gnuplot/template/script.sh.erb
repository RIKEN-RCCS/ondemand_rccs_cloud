# Set xfce environment and _VIRTUALGL
<%= set_xfce %>

# Start Gnuplot
<% if !context.global_input_file.to_s.empty? %>
xfce4-terminal -e '/usr/bin/gnuplot "<%= context.global_input_file %>" -p' &
<% else %>
xfce4-terminal -e '/usr/bin/gnuplot' &
<% end %>

# Start up xfce desktop (block until user logs out of desktop)
xfce4-session
